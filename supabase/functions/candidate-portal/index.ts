import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const token=new URL(req.url).searchParams.get('token');if(!token)throw new Error('Missing token');const db=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const {data:t,error:te}=await db.from('candidate_portal_tokens').select('candidate_id,expires_at,revoked_at').eq('token',token).single();if(te||!t||t.revoked_at||new Date(t.expires_at)<new Date())throw new Error('This candidate access link has expired or is invalid');const {data:c}=await db.from('candidates').select('id,full_name,email').eq('id',t.candidate_id).single();if(!c)throw new Error('Candidate not found');const {data:apps}=await db.from('applications').select('id,status,submitted_at,job_id,jobs(id,title,location,employment_type)').eq('candidate_id',c.id).order('submitted_at',{ascending:false});const app=apps?.[0];if(!app)throw new Error('Application not found');const {data:interviews}=await db.from('interviews').select('id,scheduled_at,duration_minutes,meeting_url,status').eq('candidate_id',c.id).order('scheduled_at');const stageMap:any={new:0,screening:1,qualified:2,submitted:3,interview:4,offer:5,placed:6,rejected:-1,withdrawn:-1};const {data:stage}=await db.from('candidates').select('stage').eq('id',c.id).single();const s=stageMap[stage?.stage||'new'];const labels=[['new','Application received'],['screening','Application screening'],['qualified','Qualified'],['submitted','Submitted to client'],['interview','Interview'],['offer','Offer / onboarding'],['placed','Placed']];return new Response(JSON.stringify({candidate:c,job:app.jobs,application:{id:app.id,status:app.status,submitted_at:app.submitted_at},timeline:labels.map(([key,label],i)=>({key,label,done:s>=i,date:i===0?app.submitted_at:null})),interviews:interviews||[]}),{headers:{...cors,'Content-Type':'application/json'}})}catch(e){return new Response(JSON.stringify({error:e instanceof Error?e.message:'Unable to load application'}),{status:400,headers:{...cors,'Content-Type':'application/json'}})}});
