import {createClient} from 'https://esm.sh/@supabase/supabase-js@2.57.0';import {PDFDocument,StandardFonts,rgb} from 'https://esm.sh/pdf-lib@1.17.1';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS'};const out=(x:any,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{...cors,'Content-Type':'application/json'}});const money=(n:any,c='GBP')=>`${String(c).trim()} ${Number(n||0).toFixed(2)}`;
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{if(req.method!=='POST')return out({error:'Method not allowed'},405);const url=Deno.env.get('SUPABASE_URL')!,anon=Deno.env.get('SUPABASE_ANON_KEY')!,service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,auth=req.headers.get('Authorization')||'';const userClient=createClient(url,anon,{global:{headers:{Authorization:auth}}});const {data:{user}}=await userClient.auth.getUser();if(!user)return out({error:'Unauthorized'},401);const db=createClient(url,service);const {data:profile}=await db.from('profiles').select('company_id,role').eq('id',user.id).single();if(!profile?.company_id||!['owner','manager'].includes(profile.role))return out({error:'Owner or manager access required'},403);const body=await req.json(),invoiceId=String(body.invoice_id||''),action=String(body.action||'generate');const {data:inv}=await db.from('invoices').select('*,clients(company_name,contact_name,email),companies(name,legal_name)').eq('id',invoiceId).eq('company_id',profile.company_id).single();if(!inv)return out({error:'Invoice not found'},404);const build=async()=>{const pdf=await PDFDocument.create(),page=pdf.addPage([595,842]),font=await pdf.embedFont(StandardFonts.Helvetica),bold=await pdf.embedFont(StandardFonts.HelveticaBold);let y=790;const line=(t:string,size=11,b=false)=>{page.drawText(t,{x:50,y,size,font:b?bold:font,color:rgb(.08,.1,.1)});y-=size+10};line(inv.companies?.legal_name||inv.companies?.name||'TalentFlow recruiter',20,true);line('INVOICE',16,true);line(`Invoice: ${inv.number}`,11,true);line(`Issue date: ${inv.issue_date||''}`);line(`Due date: ${inv.due_date||''}`);y-=15;line(`Bill to: ${inv.clients?.company_name||'Client'}`,12,true);if(inv.clients?.contact_name)line(inv.clients.contact_name);if(inv.clients?.email)line(inv.clients.email);y-=25;line(`Recruitment placement fee: ${money(inv.subtotal,inv.currency)}`,12);line(`VAT (${Number(inv.tax_rate||0).toFixed(2)}%): ${money(inv.tax_amount,inv.currency)}`,12);line(`Total: ${money(inv.total,inv.currency)}`,14,true);line(`Paid: ${money(inv.amount_paid,inv.currency)}`);line(`Amount due: ${money(inv.amount_due,inv.currency)}`,13,true);const bytes=await pdf.save(),path=`${profile.company_id}/${inv.id}/${inv.number}.pdf`;const{error}=await db.storage.from('invoice-pdfs').upload(path,bytes,{contentType:'application/pdf',upsert:true});if(error)throw error;await db.from('invoices').update({pdf_path:path,updated_at:new Date().toISOString()}).eq('id',inv.id);return path};let path=inv.pdf_path;if(!path||action==='generate')path=await build();if(action==='generate'||action==='signed_url'){const{data:s}=await db.storage.from('invoice-pdfs').createSignedUrl(path,3600);return out({ok:true,pdf_url:s?.signedUrl})}if(action==='send'){const to=String(body.to||inv.clients?.email||'').trim(),key=Deno.env.get('RESEND_API_KEY'),from=Deno.env.get('RESEND_FROM');if(!to)return out({error:'Client email is required'},400);if(!key||!from)return out({error:'Email provider is not configured'},503);const{data:s}=await db.storage.from('invoice-pdfs').createSignedUrl(path,604800);const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','Idempotency-Key':`invoice:${inv.id}:send:${inv.updated_at}`},body:JSON.stringify({from,to:[to],subject:`Invoice ${inv.number} from ${inv.companies?.name||'TalentFlow'}`,html:`<p>Please find invoice <strong>${inv.number}</strong> for <strong>${money(inv.total,inv.currency)}</strong>, due ${inv.due_date||''}.</p><p><a href="${s?.signedUrl}">View invoice PDF</a></p>`})});if(!r.ok){await db.from('invoices').update({last_delivery_error:`Resend ${r.status}`}).eq('id',inv.id);return out({error:`Email delivery failed (${r.status})`},502)}await db.from('invoices').update({status:inv.amount_due<=0?'paid':'sent',sent_at:new Date().toISOString(),sent_to:to,last_delivery_error:null,updated_at:new Date().toISOString()}).eq('id',inv.id);return out({ok:true,to})}return out({error:'Unknown action'},400)}catch(e){return out({error:e instanceof Error?e.message:'Invoice operation failed'},500)}});
