import {useEffect,useState} from 'react';import {useSearchParams,Link} from 'react-router-dom';import {CalendarDays,CheckCircle2} from 'lucide-react';import {supabase} from '../lib/supabase';import {setSeo,noIndex} from '../lib/seo';
export default function CandidatePortal(){const[sp]=useSearchParams(),token=sp.get('token')||'', [data,setData]=useState<any>(null),[loading,setLoading]=useState(true),[message,setMessage]=useState('');async function call(action='data',extra:any={}){const{data,error}=await supabase.functions.invoke('candidate-portal',{body:{action,token,...extra}});if(error)throw error;return data}useEffect(()=>{setSeo({title:'Candidate application portal | TalentFlow',description:'Secure candidate application and interview portal.',path:'/candidate',robots:noIndex});if(!token){setLoading(false);return}call().then(setData).catch(()=>setData(null)).finally(()=>setLoading(false))},[token]);async function privacy(type:string){setMessage('');try{await call('privacy_request',{request_type:type});setMessage('Your request has been recorded.')}catch(e:any){setMessage(e.message)}}if(loading)return <div className="candidate-surface"><div className="candidate-loading">Loading your application…</div></div>;if(!token||!data)return <div className="candidate-surface"><main className="candidate-simple"><Link to="/" className="marketing-brand"><span>TF</span><strong>TalentFlow</strong></Link><h1>This candidate link is unavailable.</h1><p>The link may have expired or been replaced. Contact the recruitment team handling your application for a new link.</p></main></div>;const apps=data.applications||[];const interviews=data.interviews||[];return <div className="candidate-surface"><header><Link to="/" className="marketing-brand"><span>TF</span><strong>TalentFlow Candidate Portal</strong></Link><span>Private application workspace</span></header><main><section className="candidate-welcome"><div><div className="eyebrow">YOUR APPLICATIONS</div><h1>Hi {data.candidate?.first_name||'there'}.</h1><p>Track your recruitment activity and upcoming interviews from one place.</p></div><div className="candidate-stat"><strong>{apps.length}</strong><span>application{apps.length===1?'':'s'}</span></div></section>{message&&<div className="client-message">{message}</div>}<section className="candidate-grid"><div><h2>Applications</h2><div className="candidate-list">{apps.map((a:any)=><article key={a.id}><div><small>{a.jobs?.clients?.name||'Recruitment opportunity'}</small><h3>{a.jobs?.title}</h3><span>{a.jobs?.location||'Flexible'}</span></div><div className="candidate-status"><CheckCircle2 size={15}/><span>{String(a.status||'received').replaceAll('_',' ')}</span></div></article>)}</div></div><aside><h2>Interviews</h2>{interviews.length?interviews.map((i:any)=><div className="candidate-interview" key={i.id}><CalendarDays size={18}/><div><strong>{new Date(i.scheduled_at).toLocaleString('en-GB',{dateStyle:'medium',timeStyle:'short'})}</strong><span>{i.status}</span>{i.meeting_url&&<a href={i.meeting_url} target="_blank" rel="noreferrer">Open meeting link</a>}</div></div>):<p className="candidate-muted">No interviews scheduled.</p>}<div className="candidate-privacy"><h2>Your information</h2><p>Need a copy of your information or want to make a privacy request?</p><button onClick={()=>privacy('export')}>Request my data</button><button onClick={()=>privacy('erasure')}>Request erasure</button></div></aside></section></main></div>}
