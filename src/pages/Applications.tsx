import { useEffect,useState } from 'react';
import { supabase } from '../lib/supabase';
import { Card,Badge,Empty } from '../components/Ui';
export default function Applications(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase.from('applications').select('*, jobs(title), candidates(full_name,email)').order('submitted_at',{ascending:false}).then(r=>setRows(r.data||[]))},[]);return <div className="page"><div className="page-actions"><div><h2>Applications</h2><p>Every application tied to a job and candidate record.</p></div></div>{rows.length===0?<Empty title="No applications yet" text="Publish a job and share its careers link to start receiving applications."/>:<Card><div className="table-wrap"><table><thead><tr><th>Candidate</th><th>Job</th><th>Status</th><th>Submitted</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td><strong>{a.candidates?.full_name}</strong><span>{a.candidates?.email}</span></td><td>{a.jobs?.title}</td><td><Badge tone="blue">{a.status}</Badge></td><td>{new Date(a.submitted_at).toLocaleString('en-GB')}</td></tr>)}</tbody></table></div></Card>}</div>}
