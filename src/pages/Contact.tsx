import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import AuthShell from './AuthShell';import {setSeo} from '../lib/seo';
export default function Contact(){const[name,setName]=useState(''),[email,setEmail]=useState(''),[company,setCompany]=useState(''),[message,setMessage]=useState(''),[sent,setSent]=useState(false);useEffect(()=>setSeo({title:'Book a demo | Vorlen',description:'Book a Vorlen demo or start a conversation about your recruitment operation.',path:'/contact'}),[]);function submit(e:any){e.preventDefault();const subject=encodeURIComponent('Vorlen demo request');const body=encodeURIComponent(`Name: ${name}\nEmail: ${email}\nCompany: ${company}\n\n${message}`);window.location.href=`mailto:?subject=${subject}&body=${body}`;setSent(true)}return <AuthShell eyebrow="GET STARTED" title="Book a demo" copy="Vorlen is invitation-only. Tell us about your recruitment business and we’ll help you get access.">{sent?<div className="auth-success"><strong>Your email app should now be open.</strong><p>If nothing happened, contact your Vorlen account team directly.</p><p><Link to="/login">Already invited? Sign in</Link></p></div>:<form className="customer-form" onSubmit={submit}><label>Name<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>Work email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Company<input required value={company} onChange={e=>setCompany(e.target.value)}/></label><label>What do you need?<textarea required rows={5} value={message} onChange={e=>setMessage(e.target.value)}/></label><button className="customer-primary" type="submit">Start conversation</button></form>}</AuthShell>}