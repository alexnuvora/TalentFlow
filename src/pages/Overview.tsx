import {useEffect,useMemo,useState} from 'react';
import {Link} from 'react-router-dom';
import {Card,Badge,SkeletonCards} from '../components/Ui';
import {Metric} from '../components/Metric';
import {getDashboard} from '../lib/api';
import {useWorkspaceAccess} from '../lib/access';
export default function Overview(){const[data,setData]=useState<any>(null),[error,setError]=useState('');const access=useWorkspaceAccess();useEffect(()=>{getDashboard().then(setData).catch(e=>setError(e.message))},[]);const stats=useMemo(()=>{const candidates=data?.candidates||[],jobs=data?.jobs||[],apps=data?.applications||[];return{placed:candidates.filter((c:any)=>c.stage==='placed').length,qualified:candidates.filter((c:any)=>['qualified','submitted','interview','offer','placed'].includes(c.stage)).length,published:jobs.filter((j:any)=>j.status==='published').length,candidates, jobs, apps}},[data]);if(error)return <div className="page"><div className="alert error" role="alert">{error}</div><p className="muted">TalentFlow could not load your workspace. Retry the page; if the problem persists, ask a workspace owner to check Settings.</p></div>;if(!data||access.loading)return <div className="page"><SkeletonCards count={4}/></div>;const limits=access.subscription?.limits||{},usage=access.subscription?.usage||{};const setup=[{done:(data.clients||[]).length>0,label:'Add your first client',to:'/dashboard/clients'},{done:stats.jobs.length>0,label:'Create a job',to:'/dashboard/jobs'},{done:stats.published>0,label:'Publish a role',to:'/dashboard/jobs'},{done:stats.candidates.length>0,label:'Receive or add a candidate',to:'/dashboard/candidates'}];const setupDone=setup.filter(x=>x.done).length;return <div className="page"><div className="page-actions"><div><div className="eyebrow">WORKSPACE</div><h2>Overview</h2><p>What needs attention across your recruitment operation.</p></div><div className="button-row"><Link className="button ghost" to="/dashboard/jobs">Create job</Link><Link className="button" to="/dashboard/candidates">Add candidate</Link></div></div>{access.error&&<div className="notice">Plan information could not be loaded: {access.error}</div>}<div className="metrics"><Metric label="Active jobs" value={stats.published} detail={`${stats.jobs.length} total`}/><Metric label="Candidate pipeline" value={stats.candidates.length} detail={`${stats.qualified} qualified+`}/><Metric label="Applications" value={stats.apps.length}/><Metric label="Placements" value={stats.placed}/></div><div className="grid two"><Card><div className="card-head"><div><h2>{setupDone===setup.length?'Workspace ready':'Get set up'}</h2><p>{setupDone}/{setup.length} core steps complete.</p></div><Badge tone={setupDone===setup.length?'green':'blue'}>{Math.round(setupDone/setup.length*100)}%</Badge></div>{setup.map(x=><Link className="list-row" key={x.label} to={x.to}><div><strong>{x.done?'✓ ':''}{x.label}</strong><span>{x.done?'Complete':'Open this step'}</span></div><span>→</span></Link>)}</Card><Card><div className="card-head"><div><h2>{access.subscription?.plan_name||'Subscription'}</h2><p>{access.subscription?.status==='trialing'?'Trial workspace':access.subscription?.status||'Plan status unavailable'}</p></div>{access.canManageBilling&&<Link to="/dashboard/billing">Manage</Link>}</div>{Object.keys(limits).length?<div className="plan-usage">{Object.entries(limits).map(([k,v]:any)=>{const used=Number(usage[k]||0),limit=Number(v);return <div className="usage-item" key={k}><div><strong>{k.replaceAll('_',' ')}</strong><span>{used.toLocaleString()} / {limit<0?'Unlimited':limit.toLocaleString()}</span></div>{limit>=0&&<div className="usage-track"><i style={{width:`${Math.min(100,limit?used/limit*100:0)}%`}}/></div>}</div>})}</div>:<p className="muted">Plan usage will appear here when your subscription is configured.</p>}</Card></div><div className="grid two"><Card><div className="card-head"><div><h2>Live jobs</h2><p>Roles currently accepting candidates.</p></div><Link to="/dashboard/jobs">View all</Link></div>{stats.jobs.filter((j:any)=>j.status==='published').slice(0,5).map((j:any)=><div className="list-row" key={j.id}><div><strong>{j.title}</strong><span>{j.location||'Location not set'} · {j.employment_type||'Type not set'}</span></div><Badge tone="green">Published</Badge></div>)}{stats.published===0&&<div className="empty small"><p>No published jobs yet.</p><Link to="/dashboard/jobs">Create or publish a role</Link></div>}</Card><Card><div className="card-head"><div><h2>Recent candidates</h2><p>Latest people entering your funnel.</p></div><Link to="/dashboard/candidates">View all</Link></div>{stats.candidates.slice(0,6).map((c:any)=><Link className="list-row" to={`/dashboard/candidates/${c.id}`} key={c.id}><div><strong>{c.full_name||'Unnamed candidate'}</strong><span>{c.email||'No email'} · {c.source||'Direct'}</span></div><Badge tone={c.stage==='placed'?'green':c.stage==='rejected'?'red':'blue'}>{c.stage||'new'}</Badge></Link>)}{stats.candidates.length===0&&<div className="empty small"><p>No candidates yet.</p><Link to="/dashboard/candidates">Add a candidate</Link></div>}</Card></div><Card><div className="card-head"><div><h2>Recruitment flow</h2><p>TalentFlow keeps the hand-offs visible while AI remains decision support, not the hiring decision-maker.</p></div></div><div className="funnel"><span>Traffic</span><i>→</i><span>Application</span><i>→</i><span>AI evidence</span><i>→</i><span>Recruiter review</span><i>→</i><span>Client interview</span><i>→</i><span>Placement</span></div></Card></div>}
