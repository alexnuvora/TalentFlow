import {useEffect,useState} from 'react';import {useNavigate,useParams} from 'react-router-dom';import {ArrowLeft,Printer} from 'lucide-react';import {supabase} from '../lib/supabase';import {Button,SkeletonCards} from '../components/Ui';import {noIndex,setSeo} from '../lib/seo';

export default function ClientSafeCv(){const nav=useNavigate(),{submissionId=''}=useParams(),[data,setData]=useState<any>(null),[loading,setLoading]=useState(true),[error,setError]=useState('');async function load(){setLoading(true);setError('');const{data:d,error:e}=await supabase.functions.invoke('client-candidate-cv',{body:{submission_id:submissionId}});if(e||d?.error){setData(null);setError(d?.error||e?.message||'Client-safe CV could not be opened.')}else setData(d);setLoading(false)}useEffect(()=>{setSeo({title:'Client-safe CV | Vorlen',description:'Secure client-safe candidate CV.',path:'/client/cv',robots:noIndex});void load()},[submissionId]);if(loading)return <div className="client-surface"><SkeletonCards count={2}/></div>;if(error||!data)return <div className="client-surface"><main className="candidate-simple"><div className="marketing-brand"><span>V</span><strong>Vorlen</strong></div><h1>We couldn't open this CV.</h1><p>{error}</p><Button onClick={()=>nav('/client')}>Back to client workspace</Button></main></div>;return <div className="client-surface"><header className="client-header"><div className="marketing-brand"><span>V</span><strong>Vorlen Client-Safe CV</strong></div><div><small>DIRECT CONTACT DETAILS WITHHELD</small><strong>{data.candidate_name}</strong></div></header><main><div className="page-actions"><Button variant="ghost" onClick={()=>nav('/client')}><ArrowLeft size={14}/> Back</Button><Button variant="ghost" onClick={()=>window.print()}><Printer size={14}/> Print / save PDF</Button></div><section className="client-list"><article className="client-submission"><div className="notice"><strong>Protected introduction.</strong> Direct email, phone, postal address and personal profile links are withheld. Please coordinate all contact with this candidate through Vorlen.</div><div className="client-summary"><pre style={{whiteSpace:'pre-wrap',fontFamily:'inherit',lineHeight:1.6,margin:0}}>{data.text}</pre></div></article></section></main></div>}
