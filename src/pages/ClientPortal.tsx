import {useEffect,useState} from 'react';import {supabase} from '../lib/supabase';import {Button,SkeletonCards,useToast} from '../components/Ui';import {setSeo,noIndex} from '../lib/seo';
export default function ClientPortal(){const toast=useToast();const[data,setData]=useState<any>(null),[loading,setLoading]=useState(true),[busy,setBusy]=useState(''),[feedback,setFeedback]=useState<Record<string,string>>({}),[message,setMessage]=useState(''),[error,setError]=useState('');async function load(){setLoading(true);setError('');const{data,error}=await supabase.rpc('client_portal_data');if(error){setData(null);setError(error.message||'Client workspace could not be loaded.')}else setData(data);setLoading(false)}useEffect(()=>{setSeo({title:'Client review portal | TalentFlow',description:'Secure client candidate review workspace.',path:'/client',robots:noIndex});void load()},[]);async function act(id:string,action:string){setBusy(id+action);setMessage('');setError('');const{data:result,error}=await supabase.rpc('client_portal_action',{p_submission_id:id,p_action:action,p_feedback:feedback[id]||null});setBusy('');if(error){setError(error.message);return}toast(action==='approve'?'Candidate approved.':action==='reject'?'Decision saved.':'Interview request sent.');setFeedback(f=>({...f,[id]:''}));if(result?.status)await load();else await load()}if(loading)return <div className="client-surface"><SkeletonCards count={3}/></div>;if(error&&!data)return <div className="client-surface"><main className="candidate-simple"><div className="marketing-brand"><span>TF</span><strong>TalentFlow Client Review</strong></div><h1>We couldn't open this client workspace.</h1><p>{error}</p><Button onClick={()=>void load()}>Try again</Button></main></div>;const submissions=data?.submissions||data?.candidates||[],clientName=data?.client?.company_name||data?.client?.name||'Client portal';return <div className="client-surface"><header className="client-header"><div className="marketing-brand"><span>TF</span><strong>TalentFlow Client Review</strong></div><div><small>SECURE CLIENT WORKSPACE</small><strong>{clientName}</strong></div></header><main><section className="client-intro"><div><div className="eyebrow">CANDIDATE REVIEW</div><h1>Review candidates without the email chain.</h1><p>Candidate submissions, recruiter context and your decision stay connected to the role.</p></div><div className="client-count"><strong>{submissions.length}</strong><span>candidate submission{submissions.length===1?'':'s'}</span></div></section>{message&&<div className="client-message" role="status">{message}</div>}{error&&<div className="alert error" role="alert">{error}</div>}<section className="client-list">{submissions.length?submissions.map((s:any)=>{const full=s.candidates?.full_name||[s.candidates?.first_name,s.candidates?.last_name].filter(Boolean).join(' ')||'Candidate';const summary=s.summary||s.recruiter_summary;return <article className="client-submission" key={s.id}><div className="client-submission-head"><div><small>{s.jobs?.title||'ROLE'}</small><h2>{full}</h2><p>{s.candidates?.location||'Candidate profile shared for review'}</p></div><span className="badge">{String(s.status||'submitted').replaceAll('_',' ')}</span></div>{summary&&<div className="client-summary"><strong>Recruiter summary</strong><p>{summary}</p></div>}<label className="client-feedback">Feedback<textarea rows={3} maxLength={5000} placeholder="Optional feedback for the recruiter" value={feedback[s.id]||''} onChange={e=>setFeedback(f=>({...f,[s.id]:e.target.value}))}/></label><div className="client-actions"><Button disabled={!!busy||s.status==='client_approved'} onClick={()=>act(s.id,'approve')}>Approve</Button><Button variant="ghost" disabled={!!busy||s.status==='interview_requested'} onClick={()=>act(s.id,'request_interview')}>Request interview</Button><Button variant="ghost" disabled={!!busy||s.status==='client_rejected'} onClick={()=>act(s.id,'reject')}>Not progressing</Button></div></article>}):<div className="client-empty"><h2>No candidates are waiting for review.</h2><p>New submissions from your recruitment team will appear here.</p></div>}</section>{data?.interviews?.length>0&&<section className="client-list"><h2>Interviews</h2>{data.interviews.map((i:any)=><article className="client-submission" key={i.id}><div><strong>{i.candidates?.full_name||'Candidate'}</strong><p>{i.jobs?.title||'Role'} · {new Date(i.scheduled_at).toLocaleString('en-GB',{dateStyle:'medium',timeStyle:'short'})}</p></div><span className="badge">{String(i.status||'scheduled').replaceAll('_',' ')}</span></article>)}</section>}</main></div>}
