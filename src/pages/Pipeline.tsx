import {useEffect,useState} from 'react';
import {Link} from 'react-router-dom';
import {supabase} from '../lib/supabase';
import {Badge,Card,SkeletonCards} from '../components/Ui';
const stages=['new','screening','qualified','submitted','interview','offer','placed'];
export default function Pipeline(){const[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[busy,setBusy]=useState('');async function load(){setLoading(true);setError('');const r=await supabase.from('candidates').select('id,full_name,email,stage,score,created_at').order('created_at',{ascending:false});if(r.error)setError(r.error.message);else setRows(r.data||[]);setLoading(false)}useEffect(()=>{void load()},[]);async function move(id:string,stage:string){const previous=rows.find(r=>r.id===id)?.stage;setBusy(id);setRows(x=>x.map(r=>r.id===id?{...r,stage}:r));const{error}=await supabase.from('candidates').update({stage}).eq('id',id);setBusy('');if(error){setRows(x=>x.map(r=>r.id===id?{...r,stage:previous}:r));setError(`Could not move candidate: ${error.message}`)}}if(loading)return <div className="page"><SkeletonCards count={4}/></div>;return <div className="page"><div className="page-actions"><div><h2>Pipeline</h2><p>Move candidates through a consistent, human-reviewed recruitment process.</p></div></div>{error&&<div className="alert error" role="alert">{error}</div>}<div className="kanban">{stages.map(s=>{const lane=rows.filter(r=>r.stage===s);return <div className="lane" key={s}><div className="lane-head"><strong>{s}</strong><span>{lane.length}</span></div>{lane.length===0&&<div className="empty small">No candidates</div>}{lane.map(c=><Card className="candidate-card" key={c.id}><Link className="candidate-name" to={`/dashboard/candidates/${c.id}`}><strong>{c.full_name||'Unnamed candidate'}</strong></Link><span>{c.email||'No email'}</span>{c.score!=null&&<Badge tone="blue">AI evidence score {c.score}</Badge>}<select className="stage-select" aria-label={`Candidate stage for ${c.full_name||'candidate'}`} disabled={busy===c.id} value={c.stage||'new'} onChange={e=>void move(c.id,e.target.value)}>{[...stages,'rejected','withdrawn'].map(x=><option key={x} value={x}>{x.replaceAll('_',' ')}</option>)}</select></Card>)}</div>})}</div></div>}
