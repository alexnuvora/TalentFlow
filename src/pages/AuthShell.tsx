import {Link} from 'react-router-dom';
export default function AuthShell({eyebrow,title,copy,children,footer}:{eyebrow:string;title:string;copy:string;children:React.ReactNode;footer?:React.ReactNode}){return <div className="customer-auth"><aside className="auth-story"><Link to="/" className="auth-logo"><span>TF</span><strong>TalentFlow</strong></Link><div className="auth-story-copy"><small>{eyebrow}</small><h2>Recruitment operations, in one place.</h2><p>Keep the work connected from a new role through candidate screening, client review, interview, placement and revenue.</p></div><div className="auth-proof"><div><strong>One record</strong><span>for every candidate journey</span></div><div><strong>One workflow</strong><span>from vacancy to placement</span></div><div><strong>One view</strong><span>of recruitment and revenue</span></div></div></aside><main className="auth-panel"><div className="auth-panel-inner"><Link to="/" className="auth-back">← TalentFlow</Link><div className="auth-heading"><small>{eyebrow}</small><h1>{title}</h1><p>{copy}</p></div>{children}{footer&&<div className="auth-footer-copy">{footer}</div>}</div></main></div>}
