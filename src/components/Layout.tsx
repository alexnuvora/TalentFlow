import { NavLink, useNavigate, useLocation } from 'react-router-dom';
import { BarChart3, BriefcaseBusiness, Building2, FileText, KanbanSquare, LogOut, Settings, Users, Zap, Receipt, CalendarDays, Megaphone } from 'lucide-react';
import { signOut } from '../lib/api';
const nav=[['/','Overview',BarChart3],['/jobs','Opportunities',BriefcaseBusiness],['/candidates','Candidates',Users],['/clients','Clients',Building2],['/pipeline','Pipeline',KanbanSquare],['/applications','Applications',FileText],['/interviews','Interviews',CalendarDays],['/commercial','Fees & placements',Receipt],['/acquisition','Campaigns',Megaphone],['/automations','Follow-ups',Zap],['/settings','Settings',Settings]] as const;
export function Layout({children}:{children:React.ReactNode}){const navigate=useNavigate();const {pathname}=useLocation();const title=nav.find(([to])=>to==='/'?pathname==='/':pathname.startsWith(to))?.[1]||'Workspace';return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">TF</div><div><strong>TalentFlow</strong><span>Recruitment workspace</span></div></div><nav aria-label="Workspace navigation">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>isActive?'active':''}><Icon size={18} aria-hidden="true"/><span className="nav-label">{label}</span></NavLink>)}</nav><div className="sidebar-bottom"><button className="nav-btn" onClick={async()=>{await signOut();navigate('/login')}}><LogOut size={18}/> Sign out</button></div></aside><main className="main"><header className="topbar"><div><span className="eyebrow">YOUR WORKSPACE</span><h1>{title}</h1></div><NavLink className="public-link" to="/careers">Open careers site ↗</NavLink></header>{children}</main></div>}
