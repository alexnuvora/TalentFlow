import {NavLink,useLocation,useNavigate,Navigate} from 'react-router-dom';import {useEffect,useState} from 'react';
import {LayoutDashboard,BriefcaseBusiness,Building2,FileText,KanbanSquare,LogOut,Settings,Users,Zap,Receipt,CalendarDays,Megaphone,ExternalLink,CreditCard,LockKeyhole,ChartNoAxesCombined,ListTodo,UserSearch,ShieldCheck,Handshake,LifeBuoy,UserRound,ContactRound,BrainCircuit,Menu,X} from 'lucide-react';
import {signOut} from '../lib/api';import {VorlenMark} from './VorlenBrand';
import {useWorkspaceAccess} from '../lib/access';
const nav=[['/dashboard','Overview',LayoutDashboard],['/dashboard/partner-management','Partners',Users],['/dashboard/ceo','CEO',ChartNoAxesCombined],['/dashboard/jobs','Jobs',BriefcaseBusiness],['/dashboard/candidates','Candidates',Users],['/dashboard/clients','Clients',Building2],['/dashboard/pipeline','Pipeline',KanbanSquare],['/dashboard/applications','Applications',FileText],['/dashboard/interviews','Interviews',CalendarDays],['/dashboard/commercial','Commercial',Receipt],['/dashboard/billing','Billing',CreditCard],['/dashboard/acquisition','Acquisition',Megaphone],['/dashboard/automations','Automations',Zap],['/dashboard/ai-governance','AI Governance',ShieldCheck]] as const;
export function Layout({children}:{children:React.ReactNode}){const navigate=useNavigate(),location=useLocation(),access=useWorkspaceAccess(),[mobileOpen,setMobileOpen]=useState(false);useEffect(()=>setMobileOpen(false),[location.pathname]);const plan=access.subscription?.plan_name||access.subscription?.plan_code||'Workspace';if(!access.loading&&access.role==='partner'&&!location.pathname.startsWith('/dashboard/partner'))return <Navigate to={access.partnerActive?'/dashboard/partner':'/dashboard/partner-onboarding'} replace/>;if(!access.loading&&access.role==='partner'&&!access.partnerActive&&!['/dashboard/partner-onboarding','/dashboard/partner/profile','/dashboard/partner/resources'].includes(location.pathname))return <Navigate to="/dashboard/partner-onboarding" replace/>;if(!access.loading&&access.role==='viewer')return <Navigate to="/client" replace/>;const recruiterRoutes=new Set(['/dashboard','/dashboard/jobs','/dashboard/candidates','/dashboard/pipeline','/dashboard/applications','/dashboard/interviews']);const visibleNav=access.role==='partner'?[]:access.role==='recruiter'?nav.filter(([to])=>recruiterRoutes.has(to)):nav;
const partnerNav=[
 ...(!access.partnerActive?[[ '/dashboard/partner-onboarding','Onboarding',FileText] as const]:[]),
 ...(access.partnerActive?[[ '/dashboard/partner','Home',LayoutDashboard] as const]:[]),
 ...(access.partnerCanDevelopClients?[[ '/dashboard/partner/clients','My clients',Building2] as const,[ '/dashboard/partner/crm','Account CRM',ContactRound] as const]:[]),
 ...((access.partnerCanCloseClients||access.partnerCanSourceCandidates)?[[ '/dashboard/partner/vacancies','Vacancies',BriefcaseBusiness] as const]:[]),
 ...(access.partnerCanSourceCandidates?[[ '/dashboard/partner/candidates','Candidates',UserSearch] as const,[ '/dashboard/partner/pipeline','Pipeline',KanbanSquare] as const,[ '/dashboard/partner/talent','Talent tools',BrainCircuit] as const]:[]),
 ...(access.partnerActive?[[ '/dashboard/partner/tasks','Tasks',ListTodo] as const]:[]),
 ...(access.partnerCanCloseClients?[[ '/dashboard/partner/handoffs','Commercial handoffs',Handshake] as const]:[]),
 ...(access.partnerActive?[[ '/dashboard/partner/earnings','Placements & earnings',Receipt] as const]:[]),
 ['/dashboard/partner/profile','Profile',UserRound] as const,
 ['/dashboard/partner/resources','Help & resources',LifeBuoy] as const
];const staffLinks=visibleNav.filter(([to])=>{if((to==='/dashboard/billing'||to==='/dashboard/ceo')&&!access.loading&&!access.isOwner)return false;if((to==='/dashboard/ceo'||to==='/dashboard/partner-management'||to==='/dashboard/ai-governance'||to==='/dashboard/clients'||to==='/dashboard/commercial'||to==='/dashboard/acquisition'||to==='/dashboard/automations')&&!access.loading&&!access.canManageWorkspace)return false;if(to==='/dashboard/ceo'&&!access.loading&&access.subscription?.features?.ceo_dashboard!==true)return false;return true});
const mobileLinks=access.role==='partner'?partnerNav:staffLinks;
const renderLink=([to,label,Icon]:readonly [string,string,any],mobile=false)=>{const locked=to==='/dashboard/automations'&&!access.loading&&!access.canUseAutomations;return <NavLink key={(mobile?'m-':'')+to} to={to} end={to==='/dashboard'||to==='/dashboard/partner'} onClick={()=>mobile&&setMobileOpen(false)} className={({isActive})=>`${isActive?'active':''}${locked?' plan-locked':''}`} title={locked?'Upgrade to Growth or Scale to use automations':undefined}><Icon size={16}/><span>{label}</span>{locked&&<LockKeyhole size={12}/>}</NavLink>};return <div className="app-shell"><aside className="sidebar"><div className="brand"><VorlenMark/><div><strong>VORLEN</strong><span>{access.loading?'Loading workspace…':`${plan} · ${access.role||'member'}`}</span></div></div><nav>{mobileLinks.map(x=>renderLink(x))}</nav><div className="sidebar-bottom">{access.role!=='partner'&&(access.loading||access.isOwner)&&<NavLink to="/dashboard/settings"><Settings size={16}/><span>Settings</span></NavLink>}<button className="nav-btn" onClick={async()=>{await signOut();navigate('/login')}}><LogOut size={16}/><span>Sign out</span></button></div></aside><main className="main"><header className="topbar"><button className="mobile-menu-trigger" aria-label="Open workspace menu" aria-expanded={mobileOpen} onClick={()=>setMobileOpen(true)}><Menu size={20}/></button><div className="topbar-product"><span>VORLEN</span><strong>Recruitment workspace</strong></div><NavLink className="public-link" to="/careers">Careers site <ExternalLink size={14}/></NavLink></header>{children}</main><nav className="mobile-bottom-nav" aria-label="Mobile workspace navigation"><div className="mobile-bottom-scroll">{mobileLinks.map(([to,label,Icon])=>{const locked=to==='/dashboard/automations'&&!access.loading&&!access.canUseAutomations;return <NavLink key={'bottom-'+to} to={to} end={to==='/dashboard'||to==='/dashboard/partner'} className={({isActive})=>`${isActive?'active':''}${locked?' plan-locked':''}`}><Icon size={17}/><span>{label}</span>{locked&&<LockKeyhole size={10}/>}</NavLink>})}{access.role!=='partner'&&(access.loading||access.isOwner)&&<NavLink to="/dashboard/settings"><Settings size={17}/><span>Settings</span></NavLink>}<button type="button" onClick={()=>setMobileOpen(true)} aria-label="Open all workspace navigation"><Menu size={17}/><span>More</span></button></div></nav>{mobileOpen&&<div className="mobile-nav-backdrop" onClick={()=>setMobileOpen(false)}><aside className="mobile-nav-drawer" role="dialog" aria-modal="true" aria-label="Workspace navigation" onClick={e=>e.stopPropagation()}><div className="mobile-nav-head"><div className="brand"><VorlenMark/><div><strong>VORLEN</strong><span>{plan} · {access.role||'member'}</span></div></div><button aria-label="Close workspace menu" onClick={()=>setMobileOpen(false)}><X size={20}/></button></div><nav>{mobileLinks.map(x=>renderLink(x,true))}{access.role!=='partner'&&(access.loading||access.isOwner)&&<NavLink to="/dashboard/settings" onClick={()=>setMobileOpen(false)}><Settings size={16}/><span>Settings</span></NavLink>}</nav><div className="mobile-nav-bottom"><NavLink to="/careers" onClick={()=>setMobileOpen(false)}><ExternalLink size={16}/><span>Careers site</span></NavLink><button className="nav-btn" onClick={async()=>{await signOut();setMobileOpen(false);navigate('/login')}}><LogOut size={16}/><span>Sign out</span></button></div></aside></div>}</div>}
