import { NavLink, useNavigate } from 'react-router-dom';
import { LayoutDashboard, BriefcaseBusiness, Building2, FileText, KanbanSquare, LogOut, Settings, Users, Zap, Receipt, CalendarDays, Megaphone, ExternalLink } from 'lucide-react';
import { signOut } from '../lib/api';
const nav=[['/','Overview',LayoutDashboard],['/jobs','Jobs',BriefcaseBusiness],['/candidates','Candidates',Users],['/clients','Clients',Building2],['/pipeline','Pipeline',KanbanSquare],['/applications','Applications',FileText],['/interviews','Interviews',CalendarDays],['/commercial','Commercial',Receipt],['/acquisition','Acquisition',Megaphone],['/automations','Automations',Zap]] as const;
export function Layout({children}:{children:React.ReactNode}){const navigate=useNavigate();return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">TF</div><div><strong>TalentFlow</strong><span>Recruitment operations</span></div></div><nav>{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>isActive?'active':''}><Icon size={16}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-bottom"><NavLink to="/settings"><Settings size={16}/><span>Settings</span></NavLink><button className="nav-btn" onClick={async()=>{await signOut();navigate('/login')}}><LogOut size={16}/><span>Sign out</span></button></div></aside><main className="main"><header className="topbar"><div className="topbar-product"><span>TalentFlow</span><strong>Recruitment workspace</strong></div><NavLink className="public-link" to="/careers">Careers site <ExternalLink size={14}/></NavLink></header>{children}</main></div>}
