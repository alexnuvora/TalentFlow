import { NavLink, useNavigate } from 'react-router-dom';
import { BarChart3, BriefcaseBusiness, Building2, FileText, KanbanSquare, LogOut, Settings, Users, Zap, Receipt, CalendarDays, Megaphone } from 'lucide-react';
import { signOut } from '../lib/api';
const nav=[['/','Overview',BarChart3],['/jobs','Jobs',BriefcaseBusiness],['/candidates','Candidates',Users],['/clients','Clients',Building2],['/pipeline','Pipeline',KanbanSquare],['/applications','Applications',FileText],['/interviews','Interviews',CalendarDays],['/commercial','Commercial',Receipt],['/acquisition','Acquisition',Megaphone],['/automations','Automations',Zap],['/settings','Settings',Settings]] as const;
export function Layout({children}:{children:React.ReactNode}){const navigate=useNavigate();return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">TF</div><div><strong>TalentFlow</strong><span>Performance Recruitment</span></div></div><nav>{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>isActive?'active':''}><Icon size={18}/>{label}</NavLink>)}</nav><div className="sidebar-bottom"><button className="nav-btn" onClick={async()=>{await signOut();navigate('/login')}}><LogOut size={18}/> Sign out</button></div></aside><main className="main"><header className="topbar"><div><span className="eyebrow">RECRUITMENT OPERATING SYSTEM</span><h1>Candidate acquisition, qualification & placement</h1></div><NavLink className="public-link" to="/careers">View careers site ↗</NavLink></header>{children}</main></div>}
