import fs from 'node:fs/promises';import path from 'node:path';import {loadEnv} from 'vite';
const env={...loadEnv('production',process.cwd(),''),...process.env};const dist=path.resolve('dist'),base=String(env.VITE_APP_URL||'https://talentflow-nu-neon.vercel.app').replace(/\/$/,'');const sb=env.VITE_SUPABASE_URL,key=env.VITE_SUPABASE_ANON_KEY;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const xml=s=>esc(s);const strip=s=>String(s??'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim();
async function jobs(){if(!sb||!key)return[];const fields='slug,title,description,location,employment_type,commission_text,salary_min,salary_max,application_mode,created_at',url=`${sb}/rest/v1/jobs?select=${encodeURIComponent(fields)}&status=eq.published&order=created_at.desc`;for(let attempt=1;attempt<=3;attempt++){try{const r=await fetch(url,{headers:{apikey:key,Authorization:`Bearer ${key}`},signal:AbortSignal.timeout(8000)});if(r.ok)return await r.json();console.warn('SEO build: public jobs unavailable',r.status,`attempt ${attempt}/3`)}catch(e){console.warn('SEO build: public jobs fetch failed',e instanceof Error?e.message:String(e),`attempt ${attempt}/3`)}if(attempt<3)await new Promise(resolve=>setTimeout(resolve,attempt*500))}console.warn('SEO build: continuing without dynamic job pages; runtime careers remain available.');return[]}
function inject(html,{title,description,canonical,jsonLd}){const tags=`<meta name="description" content="${esc(description)}"/><meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1"/><link rel="canonical" href="${esc(canonical)}"/><meta property="og:type" content="website"/><meta property="og:site_name" content="Vorlen"/><meta property="og:title" content="${esc(title)}"/><meta property="og:description" content="${esc(description)}"/><meta property="og:url" content="${esc(canonical)}"/><meta name="twitter:card" content="summary_large_image"/><meta name="twitter:title" content="${esc(title)}"/><meta name="twitter:description" content="${esc(description)}"/>${jsonLd?`<script type="application/ld+json">${JSON.stringify(jsonLd).replace(/</g,'\\u003c')}</script>`:''}`;return html.replace(/<title>.*?<\/title>/s,`<title>${esc(title)}</title>`).replace(/<meta name="description"[^>]*>/g,'').replace(/<link rel="canonical"[^>]*>/g,'').replace('</head>',`${tags}</head>`)}
async function writeRoute(route,html){const parts=route.split('/').filter(Boolean).map(x=>{const v=decodeURIComponent(x);if(!/^[a-z0-9][a-z0-9_-]{0,120}$/i.test(v))throw new Error('Unsafe route slug');return v});const dir=path.join(dist,...parts);await fs.mkdir(dir,{recursive:true});await fs.writeFile(path.join(dir,'index.html'),html)}
const template=await fs.readFile(path.join(dist,'index.html'),'utf8');const rows=await jobs();await writeRoute('/careers',inject(template,{title:'Open recruitment opportunities | Vorlen Careers',description:'Browse current recruitment opportunities and apply securely online through Vorlen Careers.',canonical:`${base}/careers`}));
for(const j of rows){const description=strip(j.description||`Apply for ${j.title}${j.location?` in ${j.location}`:''}.`).slice(0,155);const canonical=`${base}/careers/${encodeURIComponent(j.slug)}`;const schema=j.application_mode==='register_interest'?{'@context':'https://schema.org','@type':'WebPage',name:j.title,description:strip(j.description||j.title),url:canonical}:{'@context':'https://schema.org','@type':'JobPosting',title:j.title,description:strip(j.description||j.title),datePosted:j.created_at,employmentType:j.employment_type,url:canonical,directApply:true,jobLocation:j.location?{'@type':'Place',address:{'@type':'PostalAddress',addressLocality:j.location,addressCountry:'GB'}}:undefined};if(j.application_mode!=='register_interest'&&(j.salary_min||j.salary_max))schema.baseSalary={'@type':'MonetaryAmount',currency:'GBP',value:{'@type':'QuantitativeValue',minValue:j.salary_min||undefined,maxValue:j.salary_max||undefined,unitText:'YEAR'}};await writeRoute(`/careers/${j.slug}`,inject(template,{title:`${j.title} | Vorlen Careers`,description,canonical,jsonLd:schema}))}
const urls=[['/',new Date().toISOString()],['/careers',new Date().toISOString()],['/contact',new Date().toISOString()],['/privacy',''],['/candidate-terms',''],['/accessibility',''],...rows.map(j=>[`/careers/${j.slug}`,j.created_at||''])];const sitemap=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls.map(([u,d])=>`  <url><loc>${xml(base+u)}</loc>${d?`<lastmod>${xml(new Date(d).toISOString())}</lastmod>`:''}</url>`).join('\n')}\n</urlset>\n`;await fs.writeFile(path.join(dist,'sitemap.xml'),sitemap);await fs.writeFile(path.join(dist,'robots.txt'),`User-agent: *\nAllow: /\nDisallow: /dashboard/\nDisallow: /app/\nDisallow: /login\nDisallow: /client\nDisallow: /candidate?\nSitemap: ${base}/sitemap.xml\n`);console.log(`SEO build: generated ${rows.length} job pages and ${urls.length} sitemap URLs`);
